using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Tilemaps;

public class AnimatedTileController : MonoBehaviour
{
    //public Tilemap tilemap; // Reference to your Tilemap containing AnimatedTiles

    // void Start()
    // {
    //     if (tilemap != null)
    //     {
    //         // Loop through all tiles in the Tilemap
    //         foreach (Vector3Int pos in tilemap.cellBounds.allPositionsWithin)
    //         {
    //             TileBase tile = tilemap.GetTile(pos);
    //             if (tile is AnimatedTile animatedTile)
    //             {
    //                 // Modify the minimum and maximum speed of the AnimatedTile
    //                 animatedTile.minimumSpeed = 10f; // Example minimum speed
    //                 animatedTile.maximumSpeed = 10f; // Example maximum speed
    //             }
    //         }
    //     }
    // }
}